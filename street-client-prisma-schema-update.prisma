/**
 * Prisma Schema Update for street-client
 * 
 * Add this field to your User model in schema.prisma:
 */

model User {
  id          String   @id @default(uuid())
  githubId    Int?     @unique
  supabaseId  String?  @unique  // ← ADD THIS LINE
  email       String?  @unique
  githubLogin String?
  name        String?
  avatarUrl   String?
  accessToken String?  // Encrypted in production
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Relations
  clients     Client[]
  
  // ... rest of your fields
}

/**
 * After adding the field, run:
 * npx prisma migrate dev --name add_supabase_id
 * 
 * Or if using Prisma migrate:
 * npx prisma db push
 */

